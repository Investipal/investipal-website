---
import { featuresData } from '../../data/featuresData';
---

<section class="bg-white box-border">
  <div class="box-border px-4 md:px-8">
    <div class="box-border max-w-screen-xl w-full mx-auto">
      <div class="box-border py-16">
        <div class="text-center box-border mb-16">
          <h2 class="text-black text-3xl font-semibold box-border leading-[43.2px] font-system_ui">
            Everything financial advisors need in one platform
          </h2>
          <div class="box-border max-w-[600px] w-full mx-auto mt-6">
            <p class="text-slate-600 text-lg box-border leading-[27px] font-system_ui">
              From automated client onboarding to multi-asset portfolio construction to compliance documentation—streamline your entire workflow and grow your practice.
            </p>
          </div>
        </div>
        
        <div class="box-border space-y-16">
          {featuresData.map((feature, index) => (
            <div class={`box-border items-center gap-x-12 grid grid-cols-1 ${index % 2 === 1 ? 'md:grid-cols-[1fr_1fr]' : 'md:grid-cols-[1fr_1fr]'}`}>
              <div class={`box-border ${index % 2 === 1 ? 'md:order-2' : ''}`}>
                <div class="text-violet-600 text-sm font-medium box-border mb-3 font-system_ui">
                  {feature.badge}
                </div>
                <h3 class="text-black text-2xl font-semibold box-border mb-4 font-system_ui">
                  {feature.title}
                </h3>
                <div class="text-slate-600 text-lg box-border leading-[27px] font-system_ui">
                  {feature.id === 'faster-intake' && (
                    <p>
                      <a href="/features/automated-statement-scanner" class="text-violet-600 hover:text-violet-700 underline">AI-powered statement scanning</a> extracts portfolio data from any brokerage statement in seconds. Automated risk questionnaires, document collection, and e-signature workflows complete <a href="/features/client-onboarding" class="text-violet-600 hover:text-violet-700 underline">onboarding</a> 10x faster.
                    </p>
                  )}
                  {feature.id === 'close-business' && (
                    <p>
                      Run portfolio comparison analysis, build branded <a href="/features/client-acquisition" class="text-violet-600 hover:text-violet-700 underline">investment proposals</a>, and auto-generate <a href="/features/investment-policy-statements" class="text-violet-600 hover:text-violet-700 underline">IPS</a> and <a href="/features/regulation-best-interest-generator" class="text-violet-600 hover:text-violet-700 underline">Reg BI documentation</a>—all in one workflow. Most advisors complete proposals in under 10 minutes.
                    </p>
                  )}
                  {feature.id === 'deliver-value' && (
                    <p>
                      Construct personalized portfolios with equities, fixed income, <a href="/features/alternatives-modeling" class="text-violet-600 hover:text-violet-700 underline">alternatives</a>, and <a href="/features/annuities-insurance-modeling" class="text-violet-600 hover:text-violet-700 underline">annuities</a>. <a href="/features/asset-allocation" class="text-violet-600 hover:text-violet-700 underline">AI-powered optimization</a> for goal-based and risk-based strategies. Access our database of private securities or <a href="/features/custom-security-builder" class="text-violet-600 hover:text-violet-700 underline">model custom alternatives</a>.
                    </p>
                  )}
                </div>
              </div>
              <div class={`box-border ${index % 2 === 1 ? 'md:order-1' : ''}`}>
                <img 
                  src={feature.image} 
                  alt={feature.title} 
                  class="box-border w-full rounded-lg shadow-lg"
                />
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>
